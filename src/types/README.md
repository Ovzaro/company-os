# Types

## Why this directory exists

`types/` provides a deliberate home for small TypeScript types genuinely shared across architectural capabilities.

## What belongs here

Cross-cutting primitives, utility types, and branded identifiers with no more specific owner belong here.

## What does not belong here

A catch-all type dump, global ambient declarations without necessity, vendor types, API DTOs, and types owned by another module do not belong here.
