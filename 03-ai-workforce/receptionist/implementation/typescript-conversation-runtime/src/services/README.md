# Services

## Why this directory exists

`services/` contains cohesive reusable operations that implement domain or application responsibilities not naturally owned by an entity.

## What belongs here

Focused services with explicit dependencies, narrow contracts, and clear ownership belong here.

## What does not belong here

Generic manager classes, dependency grab bags, vendor clients, transport handlers, and logic with a better home in a capability module do not belong here.
