NODE_ENV=development
LOG_LEVEL=info
TELEGRAM_BOT_TOKEN=replace-with-your-telegram-bot-token

# The naming engine needs no credentials. Run `npm run names:live` to perform
# best-effort public RDAP and social-profile checks.
